---
layout: page
permalink: /cv/
title: CV
nav: true
nav_order: 5
description: Coming soon.
---

{% comment %}
Restore the CV layout when your data is updated:

1. Switch `layout` back to `cv`.
2. Add back optional fields, for example:
   cv_pdf: /assets/pdf/your-cv.pdf
   cv_format: rendercv   # or jsonresume
   toc:
     sidebar: left
3. Fill `_data/cv/` (RenderCV) and/or `assets/json/resume.json` (JSON Resume).

Previous template used `layout: cv` with `cv_format: rendercv` and `cv_pdf: /assets/pdf/example_pdf.pdf`.
{% endcomment %}
